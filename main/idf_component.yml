dependencies:
  idf: ^6.0
  espressif/ethernet_init:
    version: "~1.3.0"
    rules:
      - if: "target in [esp32p4]"
  scrambletools/esp_ptp:
    version: "*"
    override_path: "../components/esp_ptp"
  scrambletools/esp_avb:
    version: "*"
    override_path: "../components/esp_avb"
    rules:
      - if: "target in [esp32p4]"
